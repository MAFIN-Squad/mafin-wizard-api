{{~
    include 'commonFunctions'
    include 'csharp.commonFunctions'
    import TestsParams
    name | formatName
    path | formatPath
    usings | formatUsings 
~}}
using NUnit.Framework;

namespace {{ path | formatNamespace }};

public class {{ name }} : AbstractTest
{
    {{~ for item in sharedVariables ~}}
    {{ item }}
    {{~ end ~}}

    [SetUp]
    public void Setup()
    {
        {{ testSetup }}
    }

{{~ for test in tests ~}}
    [Test]
    public void {{test.name}}()
    {
        {{ test.body }}
    }
{{~ end ~}}
}